describe('Login Function', () => {

    it('Should be a passed login with correct credentials', () => {
        // [1] open page
        browser.url("https://www.facebook.com/");
        browser.pause(2000);
    } );
})